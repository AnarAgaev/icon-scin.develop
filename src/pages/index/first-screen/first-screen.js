$(document).ready(() => {

    // Инициализируем картинку до/после на первом экране
    let comparison = document.getElementById('comparison');
    Cocoen.create( comparison, {
        start: '52',
        color: '#ffffff'
    });
});